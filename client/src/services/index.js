import * as boards from './boards';
import * as columns from './columns';
import * as tasks from './tasks';

export const BoardsService = boards;
export const ColumnsService = columns;
export const TasksService = tasks;
